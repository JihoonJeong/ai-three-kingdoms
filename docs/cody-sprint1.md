# Cody Sprint 1 — 공개 준비

> **목표**: v1.0 공개 가능한 최소 패키지
> **기간**: ~1주
> **우선순위 순서**: 1 → 2 → 3 → 4 → 5

---

## 1. 패키징 — `npm start` 원커맨드 실행

**현재**: `npm run dev`로 vite + hono 동시 기동 (개발 모드)
**목표**: `npm start`로 빌드된 프로덕션 앱 실행

```bash
npm start
# → vite build (최초 1회 or 캐시)
# → hono 서버 기동 (port 3001, static 서빙 포함)
# → 브라우저 자동 오픈 (http://localhost:3001)
```

**요구사항**:
- `npm start` 한 번으로 빌드 + 서버 + 브라우저 오픈
- hono 서버가 vite build 결과물을 static으로 서빙 (별도 vite dev 불필요)
- 첫 실행 시 설정 마법사 자동 표시 (기존 동작 유지)

---

## 2. 설정 마법사 — 비용 안내 + 추천 순서

**현재**: 제공자 선택 (Claude/OpenAI/Gemini/Ollama) → API 키 입력 → 연결 테스트
**변경**: 비용 안내 추가 + 추천 순서 변경

### 2-1. 제공자 선택 화면 개편

```
⭐ 추천 (저비용)
  🆓 Ollama (로컬)         무료 — GPU 필요, qwen3:8b 추천
  💰 Gemini 2.0 Flash      게임당 약 $0.01 — 가장 빠르고 저렴
  💰 GPT-4o-mini           게임당 약 $0.02

🔧 다른 모델
  Gemini 3 Flash           게임당 약 $0.07
  Claude Haiku 4.5         게임당 약 $0.12
  o4-mini                  게임당 약 $0.33 (추론 토큰 과금)
  Claude Sonnet 4.5        게임당 약 $0.36

⚠️ API 사용 시 각 제공자에 의해 비용이 청구됩니다.
   위 금액은 20턴 1게임 기준 추정치이며 실제와 다를 수 있습니다.
   Ollama는 로컬 실행으로 API 비용이 발생하지 않습니다.
```

### 2-2. 구현 참고

- 기존 `web/src/screens/setup.ts` 수정
- 제공자 목록 순서: Ollama → Gemini → OpenAI → Claude (현재와 다를 수 있음)
- 각 제공자 카드에 예상 비용 표시
- "추천" 뱃지를 Ollama, Gemini Flash, GPT-4o-mini에 부여
- 하단에 비용 경고 텍스트 상시 표시

---

## 3. 데이터 수집 — 게임 결과 저장 + 공유

### 3-1. 자동 저장

게임 종료 시 (승리/패배/포기) 결과 JSON 자동 저장:

```
data/results/
  game-2026-02-20T10-30-00.json
```

**저장 내용**:
```typescript
interface GameResult {
  version: string;           // "1.0.0"
  timestamp: string;         // ISO 8601
  provider: string;          // "gemini", "ollama", etc.
  model: string;             // "gemini-2.0-flash", "qwen3:8b", etc.
  totalTurns: number;
  duration: number;          // ms
  outcome: 'victory' | 'defeat' | 'abandon';
  grade: string;             // S/A/B/C/D/F
  flags: {
    chibiVictory: boolean;
    nanjunCaptured: boolean;
    allianceFormed: boolean;
    liuBeiAlive: boolean;
  };
  actionHistory: Array<{     // 턴별 행동 요약
    turn: number;
    actions: string[];       // ["conscript:gangha:large", "march:gangha:nanjun:main"]
    aiRecommended: string[]; // AI가 추천한 액션
    followed: boolean[];     // 플레이어가 AI 추천을 따랐는지
  }>;
}
```

**주의**: 개인정보 없음. API 키 절대 포함하지 않음.

### 3-2. 게임 종료 화면 — 상세 전과 표시

현재 게임 종료 화면은 등급 + 제목 + 설명만 보여줌.
**상세 전과(stats)를 추가 표시**하여 비정석 전략 플레이어도 성취감을 느끼게:

```
┌─────────────────────────────────────────┐
│  등급: D — 패배 속의 생존                  │
│  (적벽 대전을 거치지 않았습니다)            │
│                                          │
│  ── 전과 ──                               │
│                                          │
│  ⚔️ 전투 승리: 4회                        │
│  🏯 도시 점령: 3/3 (적 도시 전부!)         │
│  👤 적장 포로: 7명                         │
│  💀 적군 격파: 38,000명                    │
│  ⏱️ 소요 턴: 12턴                         │
│  🌾 최대 병력: 45,000명                   │
│                                          │
│  "역사를 따르지 않았으나,                   │
│   역사를 넘어섰을지도 모릅니다."            │
└─────────────────────────────────────────┘
```

**구현 참고**:
- `VictoryJudge.judge()`의 `stats` 확장 필요:
  - `enemiesDefeated: number` — 격파한 적 병력 총합
  - `generalsCaptured: number` — 포로 잡은 적장 수
  - `maxTroops: number` — 최대 보유 병력 (게임 중 peak)
- 등급과 무관하게 항상 상세 전과 표시
- 등급이 D인데 도시 점령 3+ 이면 서사적 코멘트 변경 (위 예시처럼)
- GameResult JSON에도 확장된 stats 포함 (데이터 수집용)

### 3-3. 공유 버튼

게임 종료 화면 또는 설정에 "데이터 공유" 버튼:

```
📊 게임 데이터 공유
   연구에 도움을 주세요! 익명화된 게임 결과를 공유합니다.

   [결과 파일 다운로드]  [이메일로 전송]
```

- **다운로드**: JSON 파일을 로컬에 저장 (수동 첨부용)
- **이메일**: `mailto:` 링크로 미리 작성된 이메일 오픈 (첨부는 수동)
- 서버 전송 없음 — 모든 것이 클라이언트 사이드

---

## 4. 브리핑 개선 (Layer 2 최소)

**현재**: 턴 시작 시 AI 브리핑이 있지만 상황 요약이 부족
**목표**: 첫 플레이어가 헤매지 않을 정도의 상황 요약

### 4-1. 턴 시작 브리핑 강화

현재 브리핑 프롬프트에 추가:
- 직전 턴 결과 요약 (AI 세력 행동 포함)
- 현재 주요 위협/기회 1~2줄
- 남은 행동 횟수 명시

### 4-2. 첫 턴 온보딩

턴 1에서 게임 목표와 기본 조작을 간략히 안내:
- "15개 액션 중 3개를 선택합니다"
- "AI 책사의 추천을 참고하거나 무시할 수 있습니다"
- 이후 턴에서는 표시하지 않음

---

## 5. README

GitHub 메인 README 작성:

```markdown
# AI Three Kingdoms — 적벽대전

> A Human-AI cooperative strategy game.
> Part of [AI Ludens](https://jihoonjeong.github.io/ai-ludens/) Category B.

## Quick Start
npm install && npm start

## What Is This?
(2~3줄 게임 소개)

## AI Provider Setup
(설정 마법사 설명 + 비용 안내 요약)

## Cost Estimates
(모델별 비용 테이블)

## Contributing Game Data
(데이터 공유 방법)

## Research Background
(블로그 링크)

## License
MIT
```

---

## 스코프 바깥 (v1.1 이후)

아래는 이번 스프린트에서 하지 않음:
- Layer 3: 맵 확장, 적벽 보상
- Layer 4: 동맹 강화
- P2/P3 에셋
- 뱃지 시스템
- 게임 중 누적 비용 표시 (nice-to-have)
- 다중 시나리오
